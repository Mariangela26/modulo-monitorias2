package com.unisimon.monitorias.chapter.domain;

import org.junit.jupiter.api.Test;

import static org.junit.Assert.assertEquals;

class InfoSenioritysTest {

    public static final Long CHAPTERID = 1L;
    public static final String CHAPTER_NAME = "prueba";
    public static final int CHAPTER_TOTAL = 10;

    @Test
    void TestCreateInfoSeniorityModel(){
        /*InfoSeniorityTestDataBuilder infoSeniorityTestDataBuilder = new InfoSeniorityTestDataBuilder();
        InfoSenioritys infoSenioritys = infoSeniorityTestDataBuilder.buildInfoSenioritys();

        Assertions.assertEquals(CHAPTERID, infoSenioritys.getId());
        Assertions.assertEquals(CHAPTER_NAME, infoSenioritys.getChapterName());
        Assertions.assertEquals(CHAPTER_TOTAL, infoSenioritys.getChapterTotal());*/
    }
}
